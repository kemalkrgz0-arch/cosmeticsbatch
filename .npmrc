# Flat node_modules so Next.js standalone output-file tracing reliably bundles
# every dependency (pnpm's default symlinked store can leave transitive deps
# like next-intl out of .next/standalone). Required for the Docker/Coolify build.
node-linker=hoisted
